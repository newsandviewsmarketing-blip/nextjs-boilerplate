import type { Metadata } from "next";
import Link from "next/link";
import SiteHeader from "../components/SiteHeader";
import SiteFooter from "../components/SiteFooter";
import ProfilePhoto from "../components/ProfilePhoto";
import { isSupabaseConfigured } from "@/lib/supabase/config";
import { createClient } from "@/lib/supabase/server";
import { initials, sampleProfessionals, type PublicProfessional } from "@/lib/directories";

export const metadata: Metadata = { title: "Veterinary & Animal Health Professionals", description: "Discover veterinary and allied animal-health professionals across Pakistan.", alternates: { canonical: "/professionals" } };
export const dynamic = "force-dynamic";

async function loadProfessionals(): Promise<PublicProfessional[]> {
  if (!isSupabaseConfigured()) return sampleProfessionals;
  const supabase = await createClient();
  const select="user_id, slug, full_name, professional_type, headline, current_position, organization_name, city, province, district, tehsil, public_summary, years_experience, skills, profile_verified, image_url, public_phone, contact_email, address, google_maps_url";
  const [account,managed]=await Promise.all([
    supabase.from("public_professionals").select(select).order("full_name"),
    supabase.from("public_managed_professionals").select(select).order("full_name"),
  ]);
  return [...(account.data??[]),...(managed.data??[])] as PublicProfessional[];
}

export default async function ProfessionalsPage({searchParams}:{searchParams:Promise<{q?:string;type?:string;city?:string}>}){const params=await searchParams;const professionals=await loadProfessionals();const q=(params.q??"").toLowerCase();const visible=professionals.filter(p=>{const text=`${p.full_name} ${p.professional_type} ${p.headline??""} ${p.current_position??""} ${p.organization_name??""} ${p.city??""} ${p.skills.join(" ")}`.toLowerCase();return(!q||text.includes(q))&&(!params.type||params.type==="All professional types"||p.professional_type===params.type)&&(!params.city||params.city==="All cities"||p.city===params.city)});const types=[...new Set(professionals.map(x=>x.professional_type))];const cities=[...new Set(professionals.map(x=>x.city).filter(Boolean))] as string[];const showingSamples=professionals.some(x=>x.is_sample);return <main><SiteHeader/><section className="page-hero"><div className="shell"><span className="section-kicker">ANIMAL HEALTH & INDUSTRY PROFESSIONALS</span><h1>Specialists across the wider animal-health sector.</h1><p>Nutritionists, researchers, laboratory teams, academics, farm managers, regulatory professionals and other specialists are listed separately from the veterinarian directory.</p><div className="hero-actions"><Link className="button button-primary" href="/register?role=professional#registration">Create professional profile</Link></div></div></section><section className="section compact-section"><div className="shell"><div className="directory-top"><div><b>{visible.length} professional profiles</b><span>{showingSamples?"Demo mode because the backend is not configured":"Live VetConnect-reviewed directory"}</span></div><form className="market-search" method="get"><input name="q" defaultValue={params.q??""} placeholder="Name, role, sector or skill"/><select name="type" defaultValue={params.type??"All professional types"}><option>All professional types</option>{types.map(x=><option key={x}>{x}</option>)}</select><select name="city" defaultValue={params.city??"All cities"}><option>All cities</option>{cities.map(x=><option key={x}>{x}</option>)}</select><button className="button button-primary" type="submit">Search</button></form></div>{visible.length===0?<div className="empty-state"><h2>No verified professional matches these filters.</h2><Link href="/professionals">Clear search</Link></div>:<div className="company-grid">{visible.map(p=><article key={p.slug}><div className="company-mark large profile-photo-frame"><ProfilePhoto imageUrl={p.image_url} name={p.full_name} fallback={initials(p.full_name)}/></div>{p.is_sample&&<span className="sample-label">Sample profile</span>}<h3>{p.full_name}</h3><p>{p.professional_type} • {p.city||"Pakistan"}</p><div className="profile-chips">{p.skills.slice(0,3).map(skill=><span key={skill}>{skill}</span>)}</div><dl><div><dt>Current role</dt><dd>{p.current_position||"Not listed"}</dd></div><div><dt>Experience</dt><dd>{p.years_experience} years</dd></div><div><dt>Status</dt><dd>{p.profile_verified?"Verified":p.is_sample?"Sample":"Pending"}</dd></div></dl><Link className="button button-primary button-full" href={`/professionals/${p.slug}`}>View profile</Link></article>)}</div>}</div></section><section className="section section-soft"><div className="shell two-col"><div><span className="section-kicker">CLEAR PROFESSIONAL IDENTITY</span><h2>Veterinarian status is never assumed.</h2><p>Account-backed and staff-assisted professional records use the same public directory rules.</p></div><div className="spec-list"><div><b>Career-ready profiles</b><span>Skills, experience and education support future job workflows.</span></div><div><b>Assisted entry</b><span>VetConnect staff can create a profile for a professional who has not created a login account.</span></div><div><b>Privacy controls</b><span>Public summaries remain separate from private evidence and CV files.</span></div></div></div></section><SiteFooter/></main>}
