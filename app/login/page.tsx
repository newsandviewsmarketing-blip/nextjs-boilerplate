import Link from "next/link";
import SiteHeader from "../components/SiteHeader";
import SiteFooter from "../components/SiteFooter";

export default function LoginPage(){return <main><SiteHeader/><section className="auth-section"><div className="auth-shell"><div className="auth-info"><span className="section-kicker">WELCOME BACK</span><h1>Sign in to your VetConnect account.</h1><p>The role-based account model can support users, veterinarians, companies/employers and students/job seekers.</p><div className="auth-roles"><span>User / Farmer / Pet Owner</span><span>Veterinarian</span><span>Company / Employer</span><span>Student / Candidate</span></div></div><div className="auth-card"><div className="auth-tabs"><button className="active">User</button><button>Veterinarian</button><button>Company</button></div><h2>Login</h2><p>Enter your mobile number to continue.</p><label>Phone number</label><input placeholder="03XX XXXXXXX" inputMode="tel"/><label>OTP</label><div className="otp-row">{[1,2,3,4,5,6].map(i=><input key={i} maxLength={1} inputMode="numeric" aria-label={`OTP digit ${i}`}/>)}</div><button className="button button-primary button-full">Continue</button><div className="auth-divider"><span>or</span></div><button className="button button-secondary button-full">Continue with email</button><p className="auth-bottom">Don&apos;t have an account? <Link href="/register">Create account</Link></p></div></div></section><SiteFooter/></main>}
